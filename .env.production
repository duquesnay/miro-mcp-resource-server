# Scaleway Container Deployment Configuration - ADR-005 Resource Server
# This file is used when deploying to Scaleway with: ./scripts/deploy-container.sh

# Deployment target
DEPLOY_TARGET=scaleway

# Project configuration
PROJECT_NAME=miro-mcp-resource-server
REGION=fr-par

# Container Registry (MUST be configured with your Scaleway registry)
# Using fly-agile-commons (shared utilities namespace)
REGISTRY=rg.fr-par.scw.cloud/fly-agile-commons

# Container namespace (extracted from REGISTRY if not set)
NAMESPACE=fly-agile-api

# Docker image tag
TAG=latest

# Container resource limits
MIN_SCALE=1
MAX_SCALE=1
MEMORY_LIMIT=256
CPU_LIMIT=250

# Application environment variables - ADR-005 (OAuth handled by Claude.ai)
# Non-sensitive (can be in plaintext):
MIRO_CLIENT_ID=3458764647516852398
MIRO_REDIRECT_URI=https://claude.ai/api/mcp/auth_callback
BASE_URL=https://miro-mcp-resource-server.fly-agile.com
MCP_SERVER_PORT=3010

# ADR-005 NOTE: No secrets required!
# - Client secret: Managed by Claude.ai (not on our server)
# - Encryption key: Not needed (no token storage, only JWT validation)
# - Tokens: Never stored (passed in Authorization headers, validated as JWT)
