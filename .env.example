# Miro MCP Server - Environment Configuration
# Copy this file to .env and fill in your actual values

# Miro OAuth2 Configuration
# Get these from: https://miro.com/app/settings/user-profile/apps
MIRO_CLIENT_ID=your_miro_client_id_here
MIRO_CLIENT_SECRET=your_miro_client_secret_here

# OAuth2 Redirect URI
# For local testing: http://localhost:3000/oauth/callback
# For production: https://your-domain.scw.cloud/oauth/callback
MIRO_REDIRECT_URI=http://localhost:3000/oauth/callback

# Token Encryption Key (32-byte hex string)
# Generate with: openssl rand -hex 32
# IMPORTANT: Keep this secret and never commit to version control
MIRO_ENCRYPTION_KEY=generate_32_byte_hex_key_with_openssl_rand_hex_32

# HTTP Server Port (default: 3000)
MCP_SERVER_PORT=3000

# Example .env file setup:
# 1. cp .env.example .env
# 2. Generate encryption key: openssl rand -hex 32
# 3. Fill in your Miro OAuth2 credentials
# 4. Update redirect URI if needed
# 5. Run: cargo run

# Security Notes:
# - Never commit .env file to version control
# - Rotate MIRO_ENCRYPTION_KEY if compromised
# - Use different keys for development and production
# - Miro client secret should be kept confidential
